[package]
name = "py_bindings"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.19.0", features = ["extension-module"] }
runtime_core = { path = "../runtime_core" }
anyhow = "1.0"
